<div class="p-4  sm:mx-6 sm:pt-0" id="m1l3">
    @if ($latest_slide_order >= 1)
        @livewire('lesson.slides.m3l1s1', ['latest_slide_order' => $latest_slide_order])
    @endif
    @if ($latest_slide_order >= 2)
        @livewire('lesson.slides.m3l1s2', ['latest_slide_order' => $latest_slide_order])
    @endif
    @if ($latest_slide_order >= 3)
        @livewire('lesson.slides.m3l1s3', ['latest_slide_order' => $latest_slide_order])
    @endif
    @if ($latest_slide_order >= 4)
        @livewire('lesson.slides.m3l1s4', ['latest_slide_order' => $latest_slide_order])
    @endif
    @if ($latest_slide_order >= 5)
        @livewire('lesson.slides.m3l1s5', ['latest_slide_order' => $latest_slide_order])
    @endif
    @if ($latest_slide_order >= 6)
        @livewire('lesson.slides.m3l1s6', ['latest_slide_order' => $latest_slide_order])
    @endif
    @if ($latest_slide_order >= 7)
        @livewire('lesson.slides.m3l1s7', ['latest_slide_order' => $latest_slide_order])
    @endif
    @if ($latest_slide_order >= 8)
        @livewire('lesson.slides.m3l1s8', ['latest_slide_order' => $latest_slide_order])
    @endif
    @if ($latest_slide_order >= 9)
        @livewire('lesson.slides.m3l1s9', ['latest_slide_order' => $latest_slide_order])
    @endif
</div>
