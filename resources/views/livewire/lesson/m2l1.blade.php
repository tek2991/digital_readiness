<div class="p-4  sm:mx-6 sm:pt-0" id="m1l3">
    {{-- @if ($latest_slide_order >= 1)
        @livewire('lesson.slides.m2l1s1', ['latest_slide_order' => $latest_slide_order])
    @endif --}}
    <h1 class="text-3xl text-center mt-64">Module Two on the way...</h1>
</div>
